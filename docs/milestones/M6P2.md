# M6P2 — 10-second TRACKER post-TX RX window

Status: **HOST PASS; BUILD PASS; UPLOAD PASS; BOUNDED HARDWARE TIMING CHECK PASS; INTERNAL FINAL REVIEW PASS; INDEPENDENT ASTRA AUDIT OWNER-WAIVED FOR THIS SLICE.**

Baseline: `main@868b622cd07e3fe018b9f612ebc4455819a53e4f` (PR #24 merged).
Branch: `feat/m6p2-tracker-rx-window-10s`.

## Owner decision

On 2026-09-20 the product owner selected **10 seconds** as the current
development default for the bounded TRACKER post-TX LoRa receive window.

This is a deliberate availability-versus-energy tradeoff. It is intended to
leave practical rendezvous headroom after the existing 1200–4200 ms uplink
relay scheduling delay. It is not a measured battery-life optimum and is not a
guarantee that an as-yet-undefined secure downlink/ACK format will fit.

## Runtime change

`firmware/include/radio_config.h`:

```cpp
kWindowedRxAfterTxMs: 3000 -> 10000
```

No state-machine rewrite is required. The existing M6P1 invariant remains:

- a local TX terminal completion opens one fixed window;
- RX activity/error/timeout may restore RX only inside that existing deadline;
- RX does not extend the deadline;
- deadline expiry sleeps a windowed TRACKER;
- relay-forwarding-enabled and application-receiving nodes remain continuous RX;
- a local TX from sleep still wakes the radio and opens a fresh window after
  terminal completion.

## Timing rationale

Current canonical M5 figures at SF11/BW125/CR4/5/preamble 8:

- POSITION airtime: 987.136 ms;
- RELAY_FORWARD airtime: 1232.896 ms;
- uplink relay scheduling delay: 1200–4200 ms.

A worst-case currently scheduled relayed uplink can therefore finish at a
gateway at approximately:

```text
4.200 s + 1.232896 s = 5.432896 s
```

after TRACKER TX completion.

With a 10-second TRACKER window this leaves approximately:

```text
10.000 s - 5.432896 s = 4.567104 s
```

for future gateway processing and a possible return path. That remaining time is
engineering headroom only; no downlink packet or reverse forwarding runtime
exists today.

## System impact

| Area | Impact |
| --- | --- |
| TLP v1 bytes / golden fixtures | unchanged |
| Relay forwarding / dedupe | unchanged |
| RF frequency/SF/BW/CR/power | unchanged |
| BASE / relay-enabled RX policy | unchanged, continuous |
| TRACKER bounded RX window | duration changed from 3 s to 10 s; this applies after local TX terminal completion and when runtime enters WINDOWED policy |
| Storage / identity / sequence | unchanged |
| GNSS / activity / geofence | unchanged |
| BLE / flash ownership | unchanged |
| Security / ACK / commands | no new implementation |
| Power | TRACKER SX1262 RX may stay active ~7 s longer per opened bounded window (including post-TX and WINDOWED-policy entry); quantitative current impact not measured |

The energy cost scales with actual local TX cadence. The current 3-minute GNSS
tracking interval is a development default, so this setting must not be used to
claim production battery life.

## Tests

Most existing M6P1 state-machine tests derive their listen deadlines from
`kWindowedRxAfterTxMs`, so they exercise the same rollover/sleep/no-extension
logic at 10 seconds. The first owner-run M6P2 host suite exposed one legitimate
test coupling: `noSleepWhileTxOrTransitionPending()` advanced simulated time
to the listen-window duration while a TX was still active. That worked only
because the old 3000 ms listen window was shorter than the independent 5000 ms
TX software timeout. At 10000 ms the TX correctly timed out before the assertion.

The test is corrected to advance to `kTxTimeoutMs - 1` for the active-TX
invariant. Production timeout/window ordering is unchanged; this is a test
repair, not a relaxation. M6P2 additionally locks the owner-selected exact
default with a host assertion:

```cpp
assert(radio_config::kWindowedRxAfterTxMs == 10000);
```

Owner-run validation on 2026-09-20:

- `./firmware/tests/run_host_tests.sh`: **PASS** after the test-timeout coupling fix;
- includes `M6P1 portable radio listen policy checks: PASS`,
  `M6P1 RadioManager listen window checks: PASS`, M5 relay/dedupe/timing,
  R2 radio ownership/recovery, startup, R4, and M7P7A/M7P7B regression checks;
- no host failure remained in the reported run.

Owner-run build validation on 2026-09-20:

- `pio run -d firmware -e rak4630`: **SUCCESS**;
- RAM: **22,084 / 248,832 bytes (8.9%)**;
- Flash: **225,500 / 815,104 bytes (27.7%)**;
- versus the immediately prior M7P7B production image reported at
  22,084 RAM / 225,484 flash: **RAM +0 bytes, Flash +16 bytes**;
- no new compiler warning was present in the supplied build output;
- the build re-applied the pinned R4, R2.1, M7P4 and M7P7A dependency patches
  successfully.

Owner-run upload validation on 2026-09-20:

- `pio run -d firmware -e rak4630 -t upload`: **SUCCESS** on the connected
  RAK4630/RAK4631 via `nrfutil` / `/dev/ttyACM0`;
- uploaded image reports the same RAM/Flash values as the validated build:
  22,084 RAM / 225,500 flash;
- the PlatformIO udev-rules notice is a host setup warning only; DFU completed
  and reported `Device programmed.`.

Owner-run bounded hardware timing validation on 2026-09-20:

- device was first confirmed in `BASE` with
  `RADIO policy=CONTINUOUS state=CONTINUOUS`;
- after `ROLE TRACKER`, a prompt `RADIO?` query reported
  `policy=WINDOWED state=WINDOW windows=3 sleeps=2`, proving the new bounded
  window was physically open after the role transition;
- after waiting approximately 12 s, `RADIO?` reported
  `policy=WINDOWED state=ASLEEP windows=3 sleeps=3`, proving that same window
  had closed and entered sleep after the configured 10 s deadline;
- this physically exercised the shared bounded-window deadline/sleep path through
  a BASE -> TRACKER policy transition, not through an actual local-TX terminal
  event on this unit;
- the 10-second TX_DONE/TX_TIMEOUT-triggered paths are covered by the host
  RadioManager tests; the older M6P1 hardware evidence covered the same TX path
  at 3 seconds, but must not be presented as 10-second physical proof;
- this is a bounded state/timing validation, not precision timing metrology and
  not a quantitative current/battery-life measurement;
- the cumulative `rx_ms_est` diagnostic includes earlier continuous-RX time
  and is not treated as a direct 10 s measurement.

Final review disposition:

- an internal final review of the complete PR diff found no production-code
  blocker;
- two documentation-scope issues found by that review were corrected on-branch:
  the 10-second setting is the only owner-approved decision in this slice, and
  the physical test is not overstated as a real-TX-triggered 10-second proof;
- no independent Astra execution path is available in the current project/tool
  environment. On 2026-09-20 the product owner explicitly chose to proceed to
  the next stage rather than block M6P2 on that unavailable review;
- this is an **owner waiver for M6P2 only**, not a claim that an independent
  audit ran or passed.

Physical-evidence boundary: the owner-run hardware check confirms the actual
10-second bounded-window duration/sleep behavior on the RAK4630 through the same
`openListenWindow()` / deadline path used by post-TX recovery. It does not
physically prove a 10-second window triggered by a real local TX terminal event
on this no-GNSS test unit. Host tests cover that trigger path. Quantitative
current and battery-life behavior remain unmeasured.

## Non-claims

M6P2 does not implement or prove:

- gateway ACK/contact;
- gateway->relay->tracker downlink;
- selected reverse relay runtime;
- multi-gateway coordination;
- authenticated network contact;
- command delivery/result;
- quantitative current draw or battery life.

The historical M6P1 3000 ms physical evidence remains evidence for that older
tested image only.
