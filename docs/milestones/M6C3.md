# M6C3 — Bounded geofence runtime ownership

Status: **FULL HOST + RAK4630 BUILD PASS; INDEPENDENT AUDIT PENDING**.

Baseline: `main@44d3d7552092d2c9db482f74db8af3d474ef8eea`.
Branch: `feat/m6c3-geofence-runtime-seam`.

## Purpose

M6C3 adds the smallest bounded runtime/configuration owner above the already
validated M6C1 polygon geometry and M6C2 permitted-area union. It deliberately
stops before production composition-root integration.

The new `GeofenceRuntime` owns:

- one volatile bounded copy of the currently applied permitted polygons;
- all-or-nothing replacement of that runtime copy after complete validation;
- one latest valid `PermittedAreaAssessment` plus the accepted fix capture time;
- explicit clear semantics.

It does **not** own GNSS acquisition, GNSS quality/freshness, generic location
source arbitration, durable configuration, NEAR_FENCE distance, hysteresis,
repeated-fix confirmation, FREE_GRAZE, LOST, RF events or alarms.

## Runtime resource boundary

The current M6C3 runtime budget is intentionally small and explicit:

```text
maximum permitted areas:          8
maximum total effective vertices: 64
```

This is a volatile runtime resource budget, not a frozen public product limit or
a durable storage format.

M6C1 permits an optional explicit final vertex equal to the first vertex. M6C3
canonicalizes that redundant closing point when it takes ownership, so a polygon
with 64 effective vertices plus the repeated closing point still satisfies the
64-effective-vertex runtime budget. This preserves M6C1 semantics instead of
silently narrowing them at the runtime boundary.

The owned geometry consumes fixed memory only; no heap allocation is introduced.
Configuration validation remains bounded. Repeated point assessment still uses
the existing M6C2 fail-closed validation/composition path; no second geometry
implementation is introduced.

`GeofenceRuntime` is intentionally non-copyable and non-movable. Its internal
area views point into its own fixed vertex buffer, so default object copying would
create invalid ownership/lifetime semantics even though the original object was
correct. The type therefore rejects accidental copies at compile time.

## Replacement and failure semantics

`configure()` validates and sizes the complete caller-owned candidate before
changing active state.

- invalid/empty candidate -> reject;
- more than 8 areas -> reject;
- any malformed polygon -> reject;
- any per-polygon or aggregate effective-vertex budget overflow -> reject;
- successful candidate -> copy into runtime-owned fixed memory and clear the old
  assessment because it belonged to the previous geometry.

A rejected replacement preserves the previous known-good configuration **and**
its latest valid assessment. There is no partial apply.

`clear()` explicitly removes runtime configuration and invalidates the assessment.
No persistence is implied.

## Accepted-fix boundary

`observeAcceptedFix()` accepts a `GnssFix` only as a value already accepted by the
future/current location/tracking owner. M6C3 intentionally does not inspect HDOP,
satellite count, age or source ownership and therefore cannot create a competing
freshness policy.

A valid point updates the latched assessment and stores `captured_at_ms`. An
unsupported/invalid coordinate returns `kInvalidPoint` and does not erase the
last valid assessment. A defensive impossible-invalid copied configuration
returns `kConfigFault` rather than inventing OUTSIDE.

This API is a seam for later composition-root integration. In this slice there is
no authoritative geofence configuration source and no production `main.cpp`
wiring, so no field geofence behavior is claimed.

## Compatibility / system impact

```text
TLP v1 bytes/fixtures:        unchanged
RF / relay behavior:          unchanged
History / flash ownership:    unchanged
GNSS state machine/power:     unchanged
Identity / sequence:          unchanged
Legacy role behavior:         unchanged
RequestedConfig:              unchanged
Activity / RAK1904 runtime:   unchanged
BLE / DFU:                    unchanged
main.cpp production behavior: unchanged
```

No new wire packet, flash allocation, command surface, USB configuration grammar
or security semantic is introduced.

## Validation

The new owner is built by the retained M6C host sanitizer/warning profile:

```text
-std=gnu++11 -O1 -g -Wall -Wextra -Werror -fsanitize=address,undefined
```

Targeted M6C3 validation: **PASS**.

Owner-run full host regression on the final candidate tree: **PASS**. This run
includes the retained B1A/B2/B3/B4, M3/M4/M5, R2/R3/R4, M6A/M6B/M6C1/M6C2
checks plus the new M6C3 runtime checks.

M6C3 coverage includes:

- unconfigured observation cannot invent state;
- owned-copy lifetime independence from caller buffers;
- compile-time rejection of accidental runtime owner copy/move;
- multi-area INSIDE / BOUNDARY / OUTSIDE assessment;
- 64 effective vertices plus explicit repeated closing vertex;
- invalid observation preserving the previous valid assessment/time;
- malformed replacement preserving previous config/result;
- area-count and aggregate-vertex resource rejection;
- successful replacement clearing stale assessment;
- explicit clear;
- `uint32_t` capture-time wrap values retained as values without freshness
  reinterpretation.

Owner-run `pio run -e rak4630`: **SUCCESS** with the pinned production toolchain.
The existing bounded Wire and SX126x patch guards were verified/applied.

Linked image size:

```text
RAM:   14,116 / 248,832 bytes = 5.7%
Flash: 145,256 / 815,104 bytes = 17.8%
```

These values are unchanged from the merged M6B3 production image. That is
expected: M6C3 deliberately has no `main.cpp` production wiring, so the new
runtime implementation is compiled but not retained in the linked live behavior.
This is not evidence of production geofence execution.

Warning review found no new ORUN project warning. The emitted warnings are the
existing pinned third-party SX126x warnings: the library's RAK4630 `#warning` and
two signed/unsigned comparison warnings in the unused RAK11300 `SimpleTimer.cpp`
path.

Still required before merge/closure:

1. independent Astra audit of the exact candidate;
2. fixes if the audit finds real issues;
3. full host and RAK4630 revalidation after any code change;
4. merge only after the final audit/validation state is recorded accurately.

No hardware upload is required for this host-only ownership slice. Physical
geofence testing becomes meaningful only after a later reviewed slice connects a
real authoritative configuration source and accepted live location path.

## Next boundary

The next M6C work must not simply hard-code a polygon into `main.cpp` or create a
second ad-hoc USB configuration system. Production integration needs a reviewed
source of geofence intent/configuration and must pass only already-accepted live
location observations into this owner. NEAR_FENCE, hysteresis, FREE_GRAZE and
operational-state policy remain later M6C/M6D responsibilities.
